@using Shop.Model.Units;

@model IEnumerable<Unit>

@{
    ViewData["Title"] = "Index"; 
}

<partial name="_Notification" />

<div class="container p-3 ">

    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">
                لیست واحد ها
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Unit" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-file-earmark-plus"></i> ایجاد دسته جدید
            </a>
        </div>
    </div>
    <br />

    <table class="table table-striped table-bordered" style="width: 100%">
        <thead>
            <tr>
                <th>
                    شناسه
                </th>
                <th>
                    نام واحد
                </th>
                <th>
                    ویرایش
                </th>
                <th>
                    حذف
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach(var obj in Model)
            {
                <tr>
                    <td width="30%">
                        @obj.Id
                    </td>
                    <td width="30%">
                        @obj.Name
                    </td>
                    <td width="10%">
                        <div>
                            <a asp-controller="Unit" asp-action="Edit" asp-route-id="@obj.Id" class="w-50 btn-group" role="group">
                                <i class="bi bi-pencil-square"></i>
                            </a>
                        </div>
                    </td>
                    <td width="10%">
                        <div>
                            <a asp-controller="Unit" asp-action="Delete" asp-route-id="@obj.Id" class="w-50 btn-group" role="group">
                                <i class="bi bi-trash3"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>
